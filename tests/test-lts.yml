---
- hosts: all

  pre_tasks:
    - name: Install dependencies.
      package: "name={{ item }} state=present"
      with_items:
        - curl
        - unzip

  roles:
    - geerlingguy.java
    - geerlingguy.mysql
    - role_under_test
